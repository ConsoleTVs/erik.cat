---
const { class: className } = Astro.props
---

<svg
  class={className}
  stroke="currentColor"
  xmlns="http://www.w3.org/2000/svg"
  role="img"
  viewBox="0 0 64 64"
  fill="none"
  stroke-width="3"
  stroke-linecap="round"
  stroke-linejoin="round">
  <!-- Ears (filled, higher position) -->
  <path d="M20 4 L14 16 L22 12 Z" fill="currentColor" stroke="currentColor"></path>
  <path d="M44 4 L50 16 L42 12 Z" fill="currentColor" stroke="currentColor"></path>

  <!-- Nose (lowered) -->
  <path d="M30 38 L34 38 L32 41 Z" fill="currentColor" stroke="currentColor"></path>

  <!-- Whiskers Left (lowered) -->
  <path d="M24 35 L10 29"></path>
  <path d="M24 39 L10 39"></path>
  <path d="M24 43 L10 49"></path>

  <!-- Whiskers Right (lowered) -->
  <path d="M40 35 L54 29"></path>
  <path d="M40 39 L54 39"></path>
  <path d="M40 43 L54 49"></path>
</svg>
